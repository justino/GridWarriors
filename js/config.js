var config = {
    // Game Window Metrics
    width: 800,
    height: 600,
    unitSize: 16,
    discSize: 4, // Deadly is Math.round(discScale / 2)
    gridColor: 'rgba(0, 0, 0, 1)',
    
    // Game Play Params
    discSpeed: 4,
    whiteDiscPercent: 20,
    
    // Colors
    tronColor: 'rgba(255, 0, 0, 1)',
    
    // Debuging Params
};